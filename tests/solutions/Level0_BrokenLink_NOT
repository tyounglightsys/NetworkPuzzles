load Level0_BrokenLink
# The network wire between the router and laptop should be broken
# To fix it, we replace the router
# But, here we try the ping.  If this works, then links are not breaking like they are supposed to
ping pc0 laptop1
