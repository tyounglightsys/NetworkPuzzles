name: Build EXE file w/ PyInstaller GH Action
on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/*exe*"
      - "src/network_puzzles/**"
  # push:
  #   branches:
  #     - main

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6

      - name: Install Python
        uses: actions/setup-python@v6
        with:
          python-version: 3.12

      - name: Install Python dev tools
        shell: bash
        run: |
          python -m pip install -U pip
          python -m pip install wheel setuptools

      - name: Install project
        shell: bash
        run: python -m pip install .

      - name: Change slashes in src/network_puzzles/gui/networkpuzzles.kv.
        shell: bash
        run: |
          file=src/network_puzzles/gui/networkpuzzles.kv
          sed -i 's@network_puzzles/gui/@network_puzzles\\\\gui\\\\@g' "$file"
          cat "$file" > networkpuzzles_mod.log

      - name: Install PyInstaller
        shell: bash
        run: |
          python -m pip install pyinstaller
          # Install Kivy window/audio dependencies explicitly for Windows
          # python -m pip install kivy.deps.sdl2 kivy.deps.glew pypiwin32

      - name: Install OpenGL fallback
        shell: powershell
        run: |
          # Install OpenGL support
          $version = "26.1.1"
          $url = "https://github.com/pal1000/mesa-dist-win/releases/download/$version/mesa3d-$version-release-mingw.7z"
          Invoke-WebRequest -Uri "$url" -OutFile "mesa.7z"
          & "${env:ProgramFiles}/7-Zip/7z.exe" x "mesa.7z" -o"mesa" -y

      - name: Run PyInstaller
        id: pyinstaller
        shell: bash
        env:
          KIVY_NO_CONSOLELOG: 1 # avoid duplicate console logging
          KIVY_GL_BACKEND: dummy # prevents openGL error in spite of ValueError
          PYI_LOG_LEVEL: DEBUG
        run: |
          pyinstaller \
            --clean \
            --noconfirm \
            pyinstaller.spec 2>&1 | tee pyinstaller.log

      - name: Log build/dist files
        if: always()
        shell: bash
        run: |
          echo "build:" | tee -a pyinstaller-files.txt
          ls -l build/ | tee -a pyinstaller-files.txt
          echo "build/pyinstaller:" | tee -a pyinstaller-files.txt
          ls -l build/pyinstaller/ | tee -a pyinstaller-files.txt
          echo "dist:" | tee -a pyinstaller-files.txt
          ls -l dist/ | tee -a pyinstaller-files.txt

      - name: Upload Executable
        if: ${{ steps.pyinstaller.conclusion == 'success' }}
        id: artifact_upload
        uses: actions/upload-artifact@v7
        with:
          name: pyinstaller-exe
          path: ./dist
          compression-level: 0

      - name: Run PyInstaller
        id: pyinstaller-dir
        shell: bash
        env:
          KIVY_NO_CONSOLELOG: 1 # avoid duplicate console logging
          KIVY_GL_BACKEND: dummy # prevents openGL error in spite of ValueError
          PYI_LOG_LEVEL: DEBUG
        run: |
          pyinstaller \
            --clean \
            --noconfirm \
            pyinstaller-dir.spec 2>&1 | tee pyinstaller-dir.log

      - name: Log build/dist files
        if: always()
        shell: bash
        run: |
          echo "build:" | tee -a pyinstaller-dir-files.txt
          ls -l build/ | tee -a pyinstaller-dir-files.txt
          echo "build/pyinstaller:" | tee -a pyinstaller-dir-files.txt
          ls -l build/pyinstaller/ | tee -a pyinstaller-dir-files.txt
          echo "dist:" | tee -a pyinstaller-dir-files.txt
          ls -l dist/ | tee -a pyinstaller-dir-files.txt

      - name: Upload Executable
        if: ${{ steps.pyinstaller-dir.conclusion == 'success' }}
        id: artifact_upload
        uses: actions/upload-artifact@v7
        with:
          name: pyinstaller-exe
          path: ./dist
          compression-level: 0

      - name: Upload build files
        if: ${{ steps.pyinstaller.conclusion == 'success' }}
        uses: actions/upload-artifact@v7
        with:
          name: pyinstaller-build-files
          path: ./build/pyinstaller

      - name: Upload kivy & pyinstaller logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: pyinstaller-logs
          # include-hidden-files: true  # needed for .kivy dir
          path: |
            # ~\\.kivy\\logs\\*.txt  # different root; requires separate step
            *.log
            pyinstaller-files.txt
            build/pyinstaller/*.txt
